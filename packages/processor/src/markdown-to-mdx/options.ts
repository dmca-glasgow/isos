export type Options = {
  mathsAsTex: boolean;
  noWrapper: boolean;
  noSections: boolean;
};

export const defaultOptions: Options = {
  mathsAsTex: false,
  noWrapper: false,
  noSections: false,
};
